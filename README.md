Sources for the plugin `plugin.instreamatic`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.instreamatic" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
